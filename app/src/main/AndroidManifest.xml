<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.subway">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ByeActivity"></activity>
        <activity android:name=".ResultActivity" />
        <activity android:name=".SetActivity" />
        <activity android:name=".AddmenuActivity" />
        <activity android:name=".SActivity" />
        <activity android:name=".BActivity" />
        <activity android:name=".VActivity" />
        <activity android:name=".CheeseActivity" />
        <activity android:name=".AddtopActivity" />
        <activity android:name=".OrderActivity" />
        <activity android:name=".MenupActivity" />
        <activity android:name=".MenufActivity" />
        <activity android:name=".MenucActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".IntroActivity" />
        <activity android:name=".BranchActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>